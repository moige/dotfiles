# Set default values for all following accounts.
defaults
auth           on
tls            on
tls_trust_file /etc/ssl/certs/ca-certificates.crt

# Gmail
account        default
host           smtp.gmail.com
port           587
from           moige01@gmail.com
user           moige01
passwordeval    "gpg --quiet --for-your-eyes-only --no-tty --decrypt ~/.mail/email_psswd.gpg"

